import 'package:flutter/material.dart';

class UserManagementUpdatePage extends StatelessWidget {
  const UserManagementUpdatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("UPDATE"),
    );
  }
}
