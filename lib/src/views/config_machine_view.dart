import 'package:flutter/material.dart';

class ConfigMachineView extends StatelessWidget {
  const ConfigMachineView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
