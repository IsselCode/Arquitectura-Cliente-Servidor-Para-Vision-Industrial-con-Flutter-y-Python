import 'package:flutter/material.dart';

class ScanDevicesView extends StatelessWidget {
  const ScanDevicesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Scan Devices View")),
    );
  }
}
